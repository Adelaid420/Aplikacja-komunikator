{
  "name": "miku-communicator-suite",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -c magentaBright,cyanBright -n SERWER,WIDZET \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run setup && npm run dev",
    "build": "npm run build:server && npm run build:client",
    "build:server": "npm run build --prefix server",
    "build:client": "npm run build --prefix client",
    "lint": "npm run lint --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
